import { VideoPlanner } from "@/features/video-planner";

export const metadata = {
  title: "Video Token Budget Planner — tokensense",
  description: "Estimate and compare AI video processing costs across providers.",
};

export default function VideoPlannerPage() {
  return <VideoPlanner />;
}
